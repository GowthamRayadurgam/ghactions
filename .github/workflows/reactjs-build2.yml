name: React JS Build

on: 
  push:
    branches: [main]
    paths:
        ["react-proj2/**"]
  workflow_dispatch:

env:
    working-dir: "./react-proj2"

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: git checkout
              uses: actions/checkout@v6
            
            - name: install dependencies
              run: npm ci
              working-directory: ${{ env.working-dir }}

            - name: build
              run: npm run build
              working-directory: ${{ env.working-dir }}
            - name: linting
              run: npm i -g lint
              working-directory: ${{ env.working-dir }}

            - name: testing
              run: npm test
              working-directory: ${{ env.working-dir }}

    deploy:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: deploying the application
              run: echo "deploying app"

        

        